function sum(arr){
    let sum = 0;
    for(let i = 0; i < arr.length; i++){
        sum += arr[i];
    } 
    return sum;
}

function sum2(arr){
    return arr.length == 0 ? 0 : arr.shift() + sum2(arr); 
}
